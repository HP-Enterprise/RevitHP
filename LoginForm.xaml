<Window x:Class="RevitHP.LoginForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RevitHP"
        
        mc:Ignorable="d"
        Title="登录" Height="200" Width="320" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" MinWidth="320" MinHeight="200" MaxHeight="400">
    <Window.DataContext>
        <local:LoginVM/>
    </Window.DataContext>
    <Grid Height="150" VerticalAlignment="Center" Margin="4,0">
        <TextBox Name="tb_username" TabIndex="0" VerticalContentAlignment="Center" Height="23" Margin="73,12.24,22.19,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Tag=""/>
        <PasswordBox VerticalContentAlignment="Center" Name="tb_password" Margin="73,40.24,22.19,0" VerticalAlignment="Top" Height="23" Tag="" d:LayoutOverrides="VerticalAlignment"/>

        <TextBlock TextWrapping="Wrap" Text="{Binding ErrorMsg}" VerticalAlignment="Bottom" Foreground="#FFDC0202" Margin="4,0,4,4"/>
        <TextBlock HorizontalAlignment="Left" Margin="16.856,16.12,0,0" TextWrapping="Wrap" VerticalAlignment="Top"><Run Text="用户名"/><Run Text=":"/></TextBlock>
        <TextBlock HorizontalAlignment="Left" Margin="28.856,44.12,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Text="密码:"/>
        <StackPanel  Margin="0,90.084,0,0" Orientation="Horizontal" VerticalAlignment="Top" d:LayoutOverrides="Width" HorizontalAlignment="Center">
            <Button Content="登录" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
            <Button Content="取消" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="Button_Click_1" Margin="8,0,0,0" />
        </StackPanel>

    </Grid>
</Window>
